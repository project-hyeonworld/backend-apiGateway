version: '3.3'

services:
  golang-with-nginx:
    build:
      context: ./golang
      dockerfile: Dockerfile

    networks_mode: "host"
    volumes:
      - ./golang/nginx/sites-available:/etc/nginx/sites-available
      - ./golang/nginx/sites-enabled:/etc/nginx/sites-enabled